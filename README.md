# Firebase Storage Example with node js

After clone the project, installed dependencies using
```
npm install
```

#
Start server
```
npm start
```

#
Go get your firebase project config and rename to "firebase-config.json"
